export enum EDelivery {
    Home = 1,
    ConvenienceStore = 2,
}

export enum EPayment {
    CreditCard = 1,
    Cash = 2,
}

export enum EInvoice {
    Personal = 1,
    Company = 2,
}

export enum EPersonalInvoice {
    member = 1,
    barcode = 2,
}
